# Raw API Responses

This folder stores raw API responses before any normalization, tagging, scoring, or report generation.

For the first Alpha Vantage inspection test, files use:

```text
alpha_vantage_YYYYMMDD_HHMMSS.json
```

Raw files are research artifacts only. They must not trigger trades directly.

