# CHANGELOG_ATR.md

## v1.1 — 2026-04
- 修正：`#pragma once` 改為 MQL5 標準 `#ifndef` include guard

## v1.2 — 2026-04
- 修正根本 bug：`GetATR()` 唔再自己建立同釋放 handle
- 新增 `CreateATRHandle()` — caller 喺 `OnInit()` 調用一次
- 新增 `ReleaseATRHandle()` — caller 喺 `OnDeinit()` 調用
- `GetATR()` 改為接受 handle 作第一個參數
- 所有衍生 functions（`GetATRPips`, `GetSL_ATR`, `GetSL_ATR_Pips`, `GetEffectiveSL_Pips`, `GetTrailingStop_ATR`, `GetFibProximityThreshold`）同步更新 signature
- `ATR.mq5`：加入 `g_atrHandle` global variable，`OnInit()` 建立，`OnDeinit()` 釋放
- `ATR.mq5`：version 改為 1.10

## v1.0 — 2026-04
- 初版
- `GetPipSize()` — 多品種統一 pip 計算
- `GetATR()` / `GetATRPips()` — 基礎 ATR 取值
- `GetSL_ATR()` / `GetSL_ATR_Pips()` — ATR-based SL 計算
- `GetMinSL_Pips()` — 最低 SL floor（USDJPY: 6 pips）
- `GetEffectiveSL_Pips()` — max(ATR SL, min floor)
- `GetTrailingStop_ATR()` — Leg B trailing distance
- `GetFibProximityThreshold()` — ATR × 0.20 threshold
- 視覺：sub-window 三條線（ATR, SL 1.5x, SL 2.0x）
- Journal print：ATR, SL, Event SL, Trailing, Fib threshold 全部 pips
